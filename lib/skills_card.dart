import 'package:flutter/material.dart';

class skills_card extends StatelessWidget {
  const skills_card({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 250,
      color: Colors.deepPurple,
    );
  }
}
